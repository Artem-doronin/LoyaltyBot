spring:
  application:
    name: LoyaltyBot

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/bot_db
    password: pass
    username: user


  liquibase:
    change-log: db/changelog/changelog-master.yaml

  thymeleaf:
    mode: HTML
    encoding: UTF-8
    prefix: classpath:/templates/
    suffix: .html
    cache: false
    check-template-location: true
    check-template-existence: true


  mvc:
    hiddenmethod:
      filter:
        enabled: true

  telegram:
    bot:
      name: new_loyalty_bot
      token: 8892317211:AAEOJHA7a5lEUP-zA0K_ib5aJ_RwFjIcVLE

  password_generator:
    3

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    com.example.LoyaltyBot: DEBUG
    org.springframework.security.web.authentication: DEBUG
    org.springframework.security.authentication: DEBUG


outbox:
  enabled: true
  batch-size: 50
  lease-duration: 2m
  poll-interval: 5s
  reaper-interval: 1m
  max-attempts: 5
  retry:
    base: 1m
    multiplier: 2.0
    max: 1h
  sender:
    type: log







