version: '3.2'

volumes:
  pg_project:

services:
  pg_db:
    image: postgres:15-alpine
    restart: always
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=bot_db
    volumes:
      - pg_project:/var/lib/postgresql/data
    ports:
      - "5432:5432"